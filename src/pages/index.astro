---
import Layout from '@layouts/Layout.astro';
import Icon from '@components/Icon.astro';

const randomInt = (min: number, max: number) =>
		Math.floor(Math.random() * (max - min + 1)) + min;
	
	const genRandomPrettyColor = () =>
		`hsl(${randomInt(0, 360)},${randomInt(42, 98)}%,${randomInt(40, 90)}%)`;
	
	const textGradient = `45deg, ${genRandomPrettyColor()} -20%, ${genRandomPrettyColor()} 50%`;
---

<Layout
	title="React Typescript Senior Web Developer | aandre.gg"
	description={'Hello, I am Andre, always happy to help with any interesting projects!, let me know'}
>
	<main>
		<div class="outter">
			<div>
				<div class="logo-container" style={{background: `linear-gradient(${textGradient})`}}>
					<img class="logo" src="/logo-type.svg" alt="logo-type" width="40px" height="40px"/>
				</div>
				<p>Hi, I'm</p>
				<h1>Andre <br/> Kuzac</h1>
				<p>I write code and think about solutions.  👋</p>
			</div>
			<div class="inner-links">
				<a href='/Andre_Kuznetcov_resume_2023.pdf' class="resume-link" download>📖 My resume</a>
				<div>
					<a href='https://www.linkedin.com/in/andrewebdev' rel="noreferrer noopener" title='Link to my linkedin'>
						<Icon icon="linkedin"/>
					</a>
					<a href='https://github.com/KatFishSnake' rel="noreferrer noopener" title='Link to my github'>
						<Icon icon="github"/>
					</a>
				</div>
				<!-- <a href = "mailto: abc@example.com">Send Email</a> -->
			</div>
		</div>
	</main>
</Layout>

<style>
	main {
		height: 100%;
		display: flex;
		flex-direction: column;
		justify-content: center;
    align-items: center;
		width: 100%;
	}

	body {
		font-family: 'Changa', sans-serif;
	}

	h1, p {
		margin: 0;
	}

	h1 {
		line-height: 0.8;
		font-size: 72px;
	}

	p {
		font-size: 24px;
	}

	.outter {
		max-width: 680px;
		display: flex;
		margin: 0 auto;
	}

	.inner-copy {

	}

	.inner-links {
		display: flex;
		flex-direction: column;
	}

	.logo {
		width: 40px;
	}
	.logo-container {
		width: 40px;
		height: 40px;
	}

	.resume-link {
		color: black;
		text-decoration: none;
		font-size: 24px;
	}

	.resume-link:hover {
		text-decoration: underline;
	}

	.inner-links a:not(.resume-link) {
		color: #00A4D8;
		text-decoration: none;
	}
	.inner-links a:not(.resume-link):hover {
		color: #8cd1e9;
	}

	.inner-links svg {
		width: 42px;
	}

	@media (max-width: 680px) {
		.outter {
			flex-direction: column;
			align-items: flex-start;
			padding: 0 20px;
		}

		.inner-links {
			margin-top: 20px;
		}
		.resume-link {
			margin-bottom: 10px;
			font-size: 20px;
		}

		.inner-links svg {
			width: 56px;
		}
	}

</style>

